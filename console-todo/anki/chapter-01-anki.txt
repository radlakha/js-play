#separator:tab
#html:true
#notetype:Basic
#deck:Coder's Field Guide::Functional TypeScript
#tags column:3
<div class="q">What does TypeScript infer as the type of <code>version</code>, and is this reassignment valid?</div><pre>const version: number = 1.0;<br>version = 2.0;</pre><div class="options"><div class="option"><span class="opt-label">A)</span> TypeScript infers <code>number</code> &mdash; and the reassignment is valid</div><div class="option"><span class="opt-label">B)</span> TypeScript infers <code>float</code> &mdash; and the reassignment is valid</div><div class="option"><span class="opt-label">C)</span> TypeScript knows <code>version</code> is <code>number</code> &mdash; but <code>const</code> prevents reassignment, compile error</div><div class="option"><span class="opt-label">D)</span> <code>const</code> only prevents reassignment of strings &mdash; numbers can be reassigned</div></div>	<div class="answer">C &mdash; <code>const</code> cannot be reassigned regardless of type</div>	ch01 types
<div class="q">What does the <code>: void</code> return type annotation mean?</div><pre>function showMenu(): void {<br>    console.log("Menu here");<br>}</pre><div class="options"><div class="option"><span class="opt-label">A)</span> The function returns an empty string</div><div class="option"><span class="opt-label">B)</span> The function returns <code>null</code></div><div class="option"><span class="opt-label">C)</span> The function returns <code>undefined</code> explicitly</div><div class="option"><span class="opt-label">D)</span> The function does not return a value &mdash; it only performs an action</div></div>	<div class="answer">D &mdash; no return value; function only performs side effects</div>	ch01 functions types
<div class="q">What does this print for <code>tasks = [&quot;Buy milk&quot;, &quot;Walk dog&quot;]</code>?</div><pre>tasks.forEach((task, index) => {<br>    console.log(`${index + 1}. ${task}`);<br>});</pre><div class="options"><div class="option"><span class="opt-label">A)</span> <code>0. Buy milk</code> then <code>1. Walk dog</code></div><div class="option"><span class="opt-label">B)</span> <code>1. Buy milk</code> then <code>2. Walk dog</code></div><div class="option"><span class="opt-label">C)</span> <code>Buy milk. 1</code> then <code>Walk dog. 2</code></div><div class="option"><span class="opt-label">D)</span> A compile error &mdash; <code>index</code> must be typed as <code>number</code> explicitly</div></div>	<div class="answer">B &mdash; index starts at 0, +1 gives 1-based display</div>	ch01 arrays
<div class="q">Which is the correct alternative syntax for <code>string[]</code>?</div><div class="options"><div class="option"><span class="opt-label">A)</span> <code>strings[]</code></div><div class="option"><span class="opt-label">B)</span> <code>string{}</code></div><div class="option"><span class="opt-label">C)</span> <code>Array&lt;string&gt;</code></div><div class="option"><span class="opt-label">D)</span> <code>[string]</code></div></div>	<div class="answer">C &mdash; <code>Array&lt;string&gt;</code> is exactly equivalent to <code>string[]</code></div>	ch01 types arrays
<div class="q">Which operator should you use when checking user input in TypeScript?</div><pre>if (choice ___ "1") {<br>    viewTasks();<br>}</pre><div class="options"><div class="option"><span class="opt-label">A)</span> <code>=</code> &mdash; assignment operator</div><div class="option"><span class="opt-label">B)</span> <code>==</code> &mdash; loose equality, compares value only</div><div class="option"><span class="opt-label">C)</span> <code>===</code> &mdash; strict equality, compares value and type</div><div class="option"><span class="opt-label">D)</span> <code>!==</code> &mdash; not equal</div></div>	<div class="answer">C &mdash; always use <code>===</code> in TypeScript for equality checks</div>	ch01 types
<div class="q">What is the output of this template literal?</div><pre>const name = "Raman";<br>const count = 3;<br>console.log(`${name} has ${count} tasks`);</pre><div class="options"><div class="option"><span class="opt-label">A)</span> ${name} has ${count} tasks</div><div class="option"><span class="opt-label">B)</span> name has count tasks</div><div class="option"><span class="opt-label">C)</span> Raman has 3 tasks</div><div class="option"><span class="opt-label">D)</span> A compile error &mdash; you cannot mix strings and numbers in a template literal</div></div>	<div class="answer">C &mdash; expressions inside ${} are evaluated and interpolated</div>	ch01 types
<div class="q">What does <code>.push()</code> do to the array?</div><pre>let tasks: string[] = [];<br>tasks.push("Learn TypeScript");</pre><div class="options"><div class="option"><span class="opt-label">A)</span> <code>tasks</code> is type <code>Array</code> &mdash; <code>push()</code> returns the new array</div><div class="option"><span class="opt-label">B)</span> <code>tasks</code> is type <code>string[]</code> &mdash; <code>push()</code> adds to the end and <b>mutates</b> the original array</div><div class="option"><span class="opt-label">C)</span> <code>tasks</code> is type <code>string[]</code> &mdash; <code>push()</code> creates a new array and reassigns <code>tasks</code></div><div class="option"><span class="opt-label">D)</span> TypeScript cannot infer the type &mdash; you must write <code>Array&lt;string&gt;</code> explicitly</div></div>	<div class="answer">B &mdash; <code>push()</code> mutates the original array in place</div>	ch01 arrays
<span class="concept-label">Concept</span><div class="q">What are the four basic type annotations introduced in this chapter? Give one example of each.</div>	<div class="concept-answer"><b>Basic TypeScript Type Annotations</b><br><br>&bull; <code>string</code> &rarr; <code>let name: string = "Raman"</code><br>&bull; <code>number</code> &rarr; <code>const version: number = 1.0</code><br>&bull; <code>boolean</code> &rarr; <code>let isRunning: boolean = true</code><br>&bull; <code>void</code> &rarr; <code>function show(): void { ... }</code><br><br>TypeScript can often <b>infer</b> the type without annotation, but explicit annotations make intent clear and catch mistakes early.</div>	ch01 types concept
