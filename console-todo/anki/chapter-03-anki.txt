#separator:tab
#html:true
#notetype:Basic
#deck:Coder's Field Guide::Functional TypeScript
#tags column:3
<div class="q">What is the key difference between these two lines?</div><pre>// A<br>tasks.push(newTask);<br><br>// B<br>tasks = [...tasks, newTask];</pre><div class="options"><div class="option"><span class="opt-label">A)</span> A creates a new array; B modifies the existing array</div><div class="option"><span class="opt-label">B)</span> A modifies the existing array in place; B creates a new array and reassigns tasks</div><div class="option"><span class="opt-label">C)</span> Both create new arrays &mdash; they are functionally identical</div><div class="option"><span class="opt-label">D)</span> B is invalid syntax &mdash; you cannot spread an array into another array</div></div>	<div class="answer">B &mdash; push mutates in place; spread creates a new array</div>	ch03 arrays immutability
<div class="q">After this code runs, what does <code>original</code> contain?</div><pre>const original = ["Buy milk", "Walk dog"];<br>const updated = [...original, "Write report"];<br>console.log(original);</pre><div class="options"><div class="option"><span class="opt-label">A)</span> <code>["Buy milk", "Walk dog", "Write report"]</code> &mdash; spread modifies the source</div><div class="option"><span class="opt-label">B)</span> <code>[]</code> &mdash; spread moves items out of the original</div><div class="option"><span class="opt-label">C)</span> <code>["Buy milk", "Walk dog"]</code> &mdash; spread leaves the original unchanged</div><div class="option"><span class="opt-label">D)</span> A compile error &mdash; <code>original</code> is <code>const</code> so it cannot be spread</div></div>	<div class="answer">C &mdash; spread never touches the source array</div>	ch03 arrays immutability
<div class="q">What does <code>Array.map()</code> return?</div><pre>const result = tasks.map((task, i) =><br>    i === 0 ? "[✓] " + task : task<br>);</pre><div class="options"><div class="option"><span class="opt-label">A)</span> It returns nothing &mdash; it modifies <code>tasks</code> in place</div><div class="option"><span class="opt-label">B)</span> It returns a new array with the transformation applied, leaving <code>tasks</code> unchanged</div><div class="option"><span class="opt-label">C)</span> It returns the single transformed element at index 0</div><div class="option"><span class="opt-label">D)</span> It returns <code>tasks</code> with the first element removed</div></div>	<div class="answer">B &mdash; map always returns a new array; original is untouched</div>	ch03 arrays functional
<div class="q">Which of these is a pure function?</div><pre>// A<br>function addTask(tasks: string[], item: string): string[] {<br>    tasks.push(item);<br>    return tasks;<br>}<br><br>// B<br>function addTask(tasks: string[], item: string): string[] {<br>    return [...tasks, item];<br>}</pre><div class="options"><div class="option"><span class="opt-label">A)</span> A &mdash; because it returns the array</div><div class="option"><span class="opt-label">B)</span> B &mdash; because it creates a new array without modifying its input</div><div class="option"><span class="opt-label">C)</span> Both are pure &mdash; the return type is the same</div><div class="option"><span class="opt-label">D)</span> Neither &mdash; pure functions cannot accept arrays as parameters</div></div>	<div class="answer">B &mdash; no mutation, no side effects; same input always gives same output</div>	ch03 functional immutability
<div class="q">You are reading a codebase and encounter this pattern. Which style is it?</div><pre>function markComplete(tasks: string[], index: number): string[] {<br>    return tasks.map((task, i) =><br>        i === index ? "[✓] " + task : task<br>    );<br>}<br>tasks = markComplete(tasks, 2);</pre><div class="options"><div class="option"><span class="opt-label">A)</span> Imperative &mdash; it mutates the array directly</div><div class="option"><span class="opt-label">B)</span> Semi-immutable &mdash; it uses spread but still mutates tasks</div><div class="option"><span class="opt-label">C)</span> Pure functional &mdash; the function returns a new array and does not modify its inputs</div><div class="option"><span class="opt-label">D)</span> Invalid &mdash; <code>map()</code> cannot be used with string arrays</div></div>	<div class="answer">C &mdash; pure functional: new array returned, inputs untouched</div>	ch03 functional
<div class="q">The ternary is inside <code>map()</code>. What does it evaluate to when <code>i === index</code> is <code>false</code>?</div><pre>i === index ? "[✓] " + task : task</pre><div class="options"><div class="option"><span class="opt-label">A)</span> <code>undefined</code></div><div class="option"><span class="opt-label">B)</span> <code>null</code></div><div class="option"><span class="opt-label">C)</span> An empty string</div><div class="option"><span class="opt-label">D)</span> The original <code>task</code> value, unchanged</div></div>	<div class="answer">D &mdash; the false branch returns <code>task</code> as-is</div>	ch03 functional arrays
<div class="q">In which real-world scenario is the pure functional style most commonly required?</div><div class="options"><div class="option"><span class="opt-label">A)</span> Writing small CLI scripts where simplicity matters more than correctness</div><div class="option"><span class="opt-label">B)</span> State management with Redux or Zustand, where reducers must not mutate state</div><div class="option"><span class="opt-label">C)</span> Node.js servers, where performance requires in-place mutation</div><div class="option"><span class="opt-label">D)</span> TypeScript configuration files</div></div>	<div class="answer">B &mdash; Redux/Zustand reducers must return new state, never mutate</div>	ch03 functional
<span class="concept-label">Concept</span><div class="q">Describe the three main styles of updating an array in TypeScript. Which is most common in modern codebases?</div>	<div class="concept-answer"><b>Three Array Update Styles</b><br><br><b>1. Imperative (mutates):</b><br><pre>tasks.push(newTask)</pre>Simple, but modifies the original &mdash; can cause subtle bugs.<br><br><b>2. Semi-immutable</b> (most common):<br><pre>tasks = [...tasks, newTask]</pre>Creates a new array, reassigns the variable. Good balance of clarity and safety.<br><br><b>3. Pure functional</b> (cleanest):<br><pre>function add(tasks, item) { return [...tasks, item]; }</pre>No mutation anywhere. Best for testing and state management (Redux, Zustand).</div>	ch03 arrays functional concept
